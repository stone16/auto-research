## Iteration 9 vs Retained Best (Iteration 8)

**Material deltas**
- New `Skill-Catalog Margin Audit` table that explicitly names the protected rows (`sitemap-robots-generator`, `composite-campaign-build`), their required `path_reference`, and a maintenance-signal guard. This converts the iteration-7 regression risk into an auditable contract.
- Enriched `maintenance_signals` on the two protected rows: adds `no deprecation marker in extract` + concrete retry/version-check semantics (`rebuild/version-check on category or route schema change`; `retry from ResultEnvelope/action-ledger state, not blind replay`).
- Embeds the benchmark answers section directly in `knowledge_base.md`, improving self-containment for downstream judges.

**What did NOT change** — Q1–Q4 prose, Q5–Q8 row counts/columns, Q9–Q12 cognition pairs (still 5 per domain with worked/failed evidence), Q13 ≥6 shared foundations, Q14 6-row build sequence, Q15 9-row failure table. All citations and file:line anchors are byte-identical.

**Net assessment** — Defensive improvement: locks in iteration-7's cross-model evaluability gain while making the iteration-8 margin guard executable. No regressions. The flat dimensions (Architecture grounding, Cognition pairing, Cross-domain discipline, Citation discipline) are the ones that need attention next — the candidate doesn't push them forward.
