## Judge Review — Iteration 16 (restore Q3 Ads component table)

**Headline delta vs. retained best:** the candidate restores a 9-row Q3 Ads component table (`order | component | input | state | output | citation`) matching the structural shape already used for Q1 SEO/GEO. Each row terminates in a direct `repo.md:line-line` anchor (`getuai-ads.md:7-11`, `getu_ads_v2.md:9-67`, `getu_ads_v2.md:1010-1017`, `getu_ads_v2.md:1131-1149`, `getu_ads_v2.md:1048-1123`, `attribution_v2.md:13-16`, `getuai-ads-data.md:216-250`, `lawyer_marketing.md:248-269`, `growth-engine-legacy.md:83-88`). This is a meaningful Architecture-grounding gain for Ads — the previous artifact only had the q3.trace* table; now it has both a trace and a component decomposition, matching the asymmetry the retained best had between Q1 and Q3.

**Secondary gains:** Q1 closes with an explicit speed-vs-auditability tradeoff sentence (anchored to `rankncompare.md:53-56` and `growth-engine-legacy.md:43-50`/`83-88`), strengthening the disagreement framing the rubric rewards. Q15 adds a worked integration example for `q15.fm6` that triangulates cognition (`q12.cog1`), foundation (`q13.f7`/`q13.f2`), and corpus evidence (`openclaw-marketing.md:122-158`, `growth-engine-legacy.md:83-88`) — exactly the cross-question hook the Cross-domain integration dimension scores on. The benchmark answers for Q1/Q3/Q15 are updated to mirror the KB additions, preserving consistency between embedded artifacts and machine-readable answers.

**Stability:** stable IDs (`q1.trace*`, `q3.trace*`, `seo.skill1-9`, `content.skill1-8`, `ads.skill1-9`, `social.skill1-8`, `q9.cog*`-`q12.cog*`, `q13.f1-8`, `q14.m1-6`, `q15.fm1-9`) are unchanged, so prior cross-model scoring runs remain comparable. Skill counts identical (9/8/9/8 = 34 rows; protected rows `seo.skill9`, `content.skill8`, `ads.skill6`, `social.skill8` carried).

**Weakest dimension:** Cognition evidence pairing remains at 9. A handful of failed-here citations are still soft — e.g. `q12.cog2`'s failed-here points at `reddit-scount.md:141-181` for 'algorithm preference modeling', which is the same range used as a worked-here elsewhere; the contrast doesn't carry. Same pattern in `q12.cog3` (failed-here `openclaw-marketing.md:7392-7422` is also the worked-here for `q12.cog4`/`q12.cog2`). Tightening these to distinct line ranges would lift the score without restructuring.
